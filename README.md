# dokai-project

# Setup
Run `npm i` in front-end dir
Run `poetry install` in backend dir

# How to run front end 
```
cd front-end
npm run dev
```

# How to run back end 
```
cd backend
poetry run uvicorn main:app --reload
```